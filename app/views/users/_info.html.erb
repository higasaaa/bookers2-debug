<table class='table'>
  <tr><%= image_tag current_user.get_profile_image, size:'100x100' %></tr> <!--current_追加した-->
  <tr>
	  <th>name</th>
	  <th><%= current_user.name %></th> <!--current_追加した-->
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th> 
  </tr>
</table>

<div class='row'>
  <%= link_to edit_user_path(current_user), class: "btn btn-outline-secondary btn-block" do %> <!--current追加-->
    <i class="fas fa-user-cog"></i>
  <% end %>
</div>